<h1>Welcome to my Petition</h1>

<div class="info">
    <p>This is the core issue that I am most concerned about and if you sign you will learn what it is.</p>
</div>

<form class="signature" action="/petition" method="POST">
    <div class="names">
        <input type="text" name="firstname" value="{{firstname}}" placeholder="First Name">
        <input type="text" name="lastname" value="{{lastname}}" placeholder="Last Name">
        <input type="hidden" name="signatureURL" />
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </div>
    <canvas id="signature" height=100 width=400></canvas>
    <button type="submit" name="button">Submit</button>
</form>
